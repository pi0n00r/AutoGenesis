# $VENV_DIR/.env  — used by autogen.sh and systemd
# Log file path
LOG_FILE="/var/log/autogenstudio/autogen_setup.log"

# PostgreSQL credentials (script will fill AUTOGEN_DB_PASS if blank)
AUTOGEN_DB_USER="autogen_user"
AUTOGEN_DB_NAME="autogen"
AUTOGEN_DB_PASS=""

# The script regenerates DATABASE_URL at runtime (with a URL‐encoded password).
# OLLAMA:
OLLAMA_API_URL="http://localhost:11434"
OLLAMA_VERSION="v0.9.3"

# Flags (overridden by CLI)
DRY_RUN="false"
